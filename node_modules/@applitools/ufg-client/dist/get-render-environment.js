"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeGetRenderEnvironment = void 0;
const logger_1 = require("@applitools/logger");
const utils = __importStar(require("@applitools/utils"));
function makeGetRenderEnvironment({ requests, batchingTimeout = 100, logger: mainLogger }) {
    const bookedRendererWithBatching = utils.general.batchify(getRenderEnvironments, { timeout: batchingTimeout });
    return function getRenderEnvironment({ settings, logger = mainLogger, }) {
        logger = logger.extend(mainLogger, { tags: [`book-renderer-${utils.general.shortid()}`] });
        return bookedRendererWithBatching({ settings, logger });
    };
    async function getRenderEnvironments(batch) {
        const logger = (0, logger_1.mergeLoggers)(...batch.map(([{ logger }]) => logger));
        try {
            const environments = await requests.getRenderEnvironments({
                settings: batch.map(([{ settings }]) => settings),
                logger,
            });
            environments.forEach((environment, index) => {
                const [, { resolve }] = batch[index];
                resolve(environment);
            });
        }
        catch (err) {
            batch.forEach(([, { reject }]) => reject(err));
        }
    }
}
exports.makeGetRenderEnvironment = makeGetRenderEnvironment;
